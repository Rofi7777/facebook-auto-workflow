 我想在現有這三個分頁的介面下 (都不要動到錢塞個分頁的設定), 這三個功能分頁都運作正常, 千萬不要調整.
然後新增第四個分頁的功能, 第四個分頁的開發需求, 請參考下面 :

📑 產品需求規格書 (PRD)

專案名稱：BizPrompt Architect Pro (全方位 AI 指令架構師)
狀態：準備開發 (Ready for Dev)



1. 產品概述 (Executive Summary)

1.1 產品願景

打造一個 「AI 指令中樞 (AI Command Center)」，解決使用者在面對強大 AI 模型（如 ChatGPT, Gemini 3, Nano Banana）時，「不知道如何精確提問」的痛點。透過 結構化引導 與 AI 智慧轉譯，讓普通人也能產出專家級的 Prompt。

1.2 核心價值主張

	1	商業降噪：透過 4 層級引導，將模糊的商業問題轉化為帶有方法論（Framework）的專業指令。
	2	開發加速：將自然語言需求轉譯為 Gemini 3 可用的結構化 PRD 與技術棧規格。
	3	視覺精準：將畫面想像轉譯為 Nano Banana / Midjourney 專用的參數化繪圖指令。
	4	即時交付：支援一鍵複製與 Word 格式導出，無縫銜接工作流。


2. 使用者畫像 (User Personas)

角色
典型場景
核心痛點
期望功能
品牌經理 (Marketer)
需要制定 Q4 行銷策略
不懂專業模型術語 (如 AARRR, RFM)
引導式選單，自動帶入專家模型
軟體開發者 (Dev)
使用 Gemini 3 寫 Code
懶得寫完整的規格描述，導致 AI 寫錯
輸入一句話，AI 自動補全架構
內容創作者 (Creator)
使用 Nano Banana 繪圖
不知道光影、渲染引擎的英文參數
自動優化風格與畫質參數



3. 功能需求規格 (Functional Requirements)


3.1 核心模組：三態切換系統 (Mode Switching)

系統需支援三種獨立模式，透過分頁 (Tabs) 切換：
	1	👨‍💼 商業顧問 (Business Mode)
	2	💻 軟體開發 (Coding Mode - Gemini 3)
	3	🎨 視覺繪圖 (Image Mode - Nano Banana)


3.2 模式詳解


A. 商業顧問模式 (Business Logic)

	•	機制：4 層級聯動選單 (Cascading Dropdowns) + 混合輸入 (Hybrid Input)。
	•	層級定義：
	◦	L1 領域 (Domain)：如市場行銷、企業戰略。
	◦	L2 賽道 (Industry)：如 SaaS B2B、電商零售。
	◦	L3 角色 (Role)：如 成長駭客、產品經理。
	◦	L4 模型 (Framework)：如 AARRR、SWOT、GTM。
	•	自定義規則：每一層級皆需包含 [➕ 自定義 / Custom] 選項。選擇後，UI 需滑出輸入框供使用者手動輸入，並將該輸入值用於 Prompt 組裝。
	•	Prompt 輸出：基於靜態模板組裝（不需 API）。

B. 軟體開發模式 (Coding Logic)

	•	機制：自然語言輸入 -> Gemini API (Middleware) -> 結構化輸出。
	•	輸入：使用者輸入模糊需求（例：「做一個像 Uber 的 App」）。
	•	處理：後端呼叫 Gemini，角色設定為「資深架構師」。
	•	輸出格式：Markdown 格式，包含：
	1	Tech Stack (前端/後端/資料庫)
	2	Core Features (核心功能列表)
	3	Database Schema Concept (簡易資料結構)
	4	UI/UX Guidelines

C. 視覺繪圖模式 (Image Logic)

	•	機制：自然語言輸入 -> Gemini API (Middleware) -> 參數化輸出。
	•	輸入：使用者描述畫面（例：「太空中的貓」）。
	•	處理：後端呼叫 Gemini，角色設定為「Prompt Engineer」。
	•	輸出格式：純英文指令，包含：
	◦	(Subject) + (Art Style) + (Lighting) + (Camera) + (Quality Tags) + (--ar ratio)


3.3 工具模組 (Utilities)

	•	一鍵複製 (Copy to Clipboard)：將生成的文字複製到剪貼簿，並顯示 "Copied" 提示。
	•	導出 Word (Export to .docx)：
	◦	使用 docx 套件在前端生成。
	◦	文件結構：標題 (Project Name) -> 原始需求 -> 生成之結構化 Prompt (使用等寬字體) -> 生成時間。
	◦	檔名規則：Prompt_{Mode}_{Date}.docx。


4. 資訊架構與流程 (Information Architecture)

程式碼片段

graph TD
    User[使用者進入] --> ModeSelect{選擇模式}
    
    %% 商業模式流程
    ModeSelect -->|商業顧問| L1[L1: 領域選擇]
    L1 --> L2[L2: 行業選擇]
    L2 --> L3[L3: 角色選擇]
    L3 --> L4[L4: 模型選擇]
    L4 --> Context[輸入具體任務]
    Context --> GenBiz[前端組裝 Prompt]
    
    %% AI 模式流程
    ModeSelect -->|軟體開發 / 視覺繪圖| TextInput[輸入自然語言需求]
    TextInput --> CallAPI[呼叫後端 API /api/refine]
    CallAPI --> Gemini[Google Gemini Pro]
    Gemini --> ReturnPrompt[回傳結構化指令]
    
    %% 輸出流程
    GenBiz --> Display[顯示結果區]
    ReturnPrompt --> Display
    
    Display --> Action1[複製文字]
    Display --> Action2[下載 Word]


5. 技術架構規格 (Technical Architecture)


5.1 技術棧 (Tech Stack)

	•	Frontend Framework: Next.js 14+ (App Router)
	•	Language: TypeScript
	•	Styling: Tailwind CSS (響應式設計)
	•	Icons: Lucide React
	•	AI Integration: Google Generative AI SDK (@google/generative-ai)
	•	Document Gen: docx, file-saver

5.2 資料結構 (Data Schema) - 商業模式用

TypeScript

// types/index.ts
export interface CategoryNode {
  id: string;
  label: string;
  children?: CategoryNode[];
}
// 支援巢狀結構，存儲於 lib/data.ts
