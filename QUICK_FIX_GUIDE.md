# 快速修复指南 - 首页和Page4显示问题

## 🔍 问题诊断

如果遇到以下问题：
1. **首页无法正常显示**
2. **AI指令框架（Page 4）没有出现内容**

## 🛠️ 快速修复步骤

### 步骤1: 检查浏览器控制台

1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签
3. 检查是否有以下错误：
   - 404错误（文件未找到）
   - JavaScript错误
   - CSS加载错误

### 步骤2: 使用诊断工具

1. 访问：`http://localhost:3000/debug-check.html`
2. 点击"运行诊断"按钮
3. 查看诊断结果，找出问题所在

### 步骤3: 常见问题修复

#### 问题1: CSS文件未加载

**症状**: 页面样式混乱

**修复**:
1. 检查 `public/index.html` 中的CSS路径
2. 确保路径是 `/css/` 而不是 `/public/css/`
3. 清除浏览器缓存（Ctrl+F5 或 Cmd+Shift+R）

#### 问题2: JavaScript模块未加载

**症状**: 功能不工作，控制台显示模块未定义

**修复**:
1. 检查 `public/index.html` 中的JS路径
2. 确保路径是 `/js/` 而不是 `/public/js/`
3. 检查文件是否存在

#### 问题3: Page 1 不显示

**症状**: 首页空白或无法看到内容

**修复**:
```javascript
// 在浏览器控制台运行：
const page1 = document.getElementById('page1');
if (page1) {
  page1.classList.add('active');
  page1.style.display = 'block';
  page1.style.visibility = 'visible';
  page1.style.opacity = '1';
}
```

#### 问题4: Page 4 内容不显示

**症状**: 切换到Page 4后看不到表单内容

**修复**:
```javascript
// 在浏览器控制台运行：
const page4 = document.getElementById('page4');
if (page4) {
  page4.classList.add('active');
  page4.style.display = 'block';
  page4.style.visibility = 'visible';
  page4.style.opacity = '1';
  
  // 确保容器可见
  const containers = page4.querySelectorAll('.workflow-container, .input-panel, .output-panel, .prompt-mode');
  containers.forEach(c => {
    c.style.display = c.classList.contains('workflow-container') ? 'grid' : 'block';
    c.style.visibility = 'visible';
    c.style.opacity = '1';
  });
  
  // 确保business模式可见
  const businessMode = page4.querySelector('#businessMode');
  if (businessMode) {
    businessMode.style.display = 'block';
  }
}
```

## 🔧 手动修复检查清单

### 检查1: 文件路径
- [ ] CSS文件路径正确（`/css/` 而不是 `/public/css/`）
- [ ] JS文件路径正确（`/js/` 而不是 `/public/js/`）
- [ ] 所有文件都存在

### 检查2: 页面元素
- [ ] `#page1` 元素存在
- [ ] `#page1` 有 `active` 类
- [ ] `#page4` 元素存在
- [ ] `#page4` 的容器元素存在

### 检查3: CSS样式
- [ ] `.page-content.active` 样式是 `display: block`
- [ ] `#page4 .workflow-container` 样式是 `display: grid`
- [ ] `#page4 .input-panel` 和 `.output-panel` 样式是 `display: block`

### 检查4: JavaScript初始化
- [ ] `Page1ProductAnalysis` 模块已初始化
- [ ] `Page4PromptArchitect` 模块已初始化
- [ ] `Router` 模块正常工作
- [ ] `switchPage` 函数正常工作

## 🚀 快速测试

### 测试首页显示
1. 打开应用
2. 应该看到Page 1的内容
3. 应该能看到产品信息输入表单
4. 应该能看到上传区域

### 测试Page 4显示
1. 点击"AI 指令架構師"导航按钮
2. 应该看到Page 4的内容
3. 应该能看到三个模式切换按钮
4. 应该能看到商业顾问模式的表单
5. 应该能看到右侧的输出面板

## 📞 如果问题仍然存在

1. **检查浏览器控制台**：查看具体错误信息
2. **使用诊断工具**：运行 `debug-check.html`
3. **检查网络请求**：查看Network标签，确认文件加载成功
4. **清除缓存**：强制刷新页面（Ctrl+F5）

## 🔄 重置方法

如果所有方法都失败，可以尝试：

1. **清除浏览器缓存**
2. **重新加载页面**（Ctrl+F5）
3. **检查服务器是否运行**
4. **检查文件权限**

---

**最后更新**: 2024年12月

