# 🔧 修复 404 错误

## 问题诊断

你遇到了 404 错误，这通常是因为：

1. **服务器没有在正确的目录运行**
2. **访问的路径不正确**
3. **服务器配置问题**

## ✅ 解决方案

### 方法 1: 停止旧服务器，重新启动（推荐）

#### 步骤 1: 停止当前服务器

在终端运行：

```bash
# 停止占用端口 8000 的进程
kill 8101

# 或者停止所有 Python HTTP 服务器
pkill -f "http.server"
```

#### 步骤 2: 在正确的目录启动服务器

```bash
# 确保在项目根目录
cd /Users/rofi/Desktop/App/GooGa-Ai-Hub

# 启动服务器（必须在 public 目录的父目录）
python3 -m http.server 8000
```

**重要**：服务器必须在 `GooGa-Ai-Hub` 目录运行，**不是**在 `public` 目录！

#### 步骤 3: 访问正确的 URL

在浏览器访问：

```
http://localhost:8000/public/test-architecture.html
```

注意：URL 中需要包含 `/public/` 路径！

### 方法 2: 在 public 目录启动服务器（更简单）

如果你想直接访问文件而不需要 `/public/` 路径：

```bash
# 进入 public 目录
cd /Users/rofi/Desktop/App/GooGa-Ai-Hub/public

# 启动服务器
python3 -m http.server 8000
```

然后访问：

```
http://localhost:8000/test-architecture.html
```

### 方法 3: 使用提供的脚本

```bash
cd /Users/rofi/Desktop/App/GooGa-Ai-Hub
./start-test-server.sh
```

脚本会自动处理目录问题。

## 📝 正确的访问路径

### 如果服务器在 `GooGa-Ai-Hub` 目录运行：

```
http://localhost:8000/public/test-architecture.html
http://localhost:8000/public/test-integration.html
http://localhost:8000/public/index.html
```

### 如果服务器在 `public` 目录运行：

```
http://localhost:8000/test-architecture.html
http://localhost:8000/test-integration.html
http://localhost:8000/index.html
```

## 🔍 检查服务器是否正常运行

在浏览器访问：

```
http://localhost:8000/
```

如果看到文件列表或目录结构，说明服务器正常运行。

## ⚠️ 常见错误

### 错误 1: 服务器在错误的目录

**症状**: 404 错误

**解决**: 确保服务器在正确的目录运行

### 错误 2: 路径中缺少 /public/

**症状**: 404 错误

**解决**: 在 URL 中添加 `/public/` 路径

### 错误 3: 端口被占用

**症状**: 无法启动服务器

**解决**: 
```bash
# 停止占用端口的进程
kill 8101

# 或使用其他端口
python3 -m http.server 8001
```

## 🚀 快速修复命令

```bash
# 1. 停止旧服务器
pkill -f "http.server"

# 2. 进入项目目录
cd /Users/rofi/Desktop/App/GooGa-Ai-Hub

# 3. 启动服务器
python3 -m http.server 8000

# 4. 在浏览器访问
# http://localhost:8000/public/test-architecture.html
```

---

**试试这些方法，应该可以解决问题！** 🎯


